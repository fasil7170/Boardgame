<build>
    <plugins>
        <!-- Maven Compiler Plugin: compile Java source -->
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>3.11.0</version>
            <configuration>
                <source>17</source> <!-- use the Java version you target -->
                <target>17</target>
            </configuration>
        </plugin>

        <!-- Maven Surefire Plugin: run unit tests -->
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <version>3.1.2</version>
        </plugin>

        <!-- Maven Jar Plugin: package project as a jar -->
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-jar-plugin</artifactId>
            <version>3.3.0</version>
            <configuration>
                <archive>
                    <manifest>
                        <addClasspath>true</addClasspath>
                        <mainClass>com.example.MainClass</mainClass> <!-- replace with your main class if any -->
                    </manifest>
                </archive>
            </configuration>
        </plugin>
    </plugins>
</build>
